CREATE TABLE IF NOT EXISTS user_feedback (id SERIAL PRIMARY KEY, user_id INTEGER, user_name TEXT NOT NULL DEFAULT '', text TEXT NOT NULL, rating SMALLINT, category TEXT NOT NULL DEFAULT 'general', created_at TIMESTAMPTZ NOT NULL DEFAULT NOW());